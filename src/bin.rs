/// The Bin to the placed into
#[derive(Debug)]
pub struct Bin {
    /// Height
    pub heigth: f32,
    /// Width
    pub width: f32,
    /// Length
    pub length: f32,
    /// Weight
    max_weight: f32,
}
